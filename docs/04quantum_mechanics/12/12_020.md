---
tags:
  - 標準
---

# 経路積分の導入

## 問題
時間発展演算子を細かく分割し，各時間ステップの間に位置の完全系を挿入していく手続きにより，量子力学の状態遷移確率をすべての可能な経路の足し合わせとして表す経路積分表示が導かれる。

質量 $m$ ，ポテンシャル $V(x)$ の中を運動する粒子のハミルトニアンを $\hat{H} = \hat{K} + \hat{V}$ とする。ここで運動エネルギー項は $\hat{K} = \hat{p}^2 / 2m$ である。時刻 $t_i$ から $t_f$ までの時間を $N$ 等分し，微小な時間間隔を $\epsilon = (t_f - t_i)/N$ と置く。このとき，伝播核は次のように積の形式に分割できる。

$$
K(x_f, t_f; x_i, t_i) = \left\langle x_f \left| \left( \exp\left(-i \hat{H} \epsilon / \hbar\right) \right)^N \right| x_i \right\rangle
$$

以下の問いに答えよ。

(1) $j = 1, 2, \dots, N-1$ に対する中間時刻を $t_j = t_i + j\epsilon$ とする。位置固有状態の完全性関係 $\int |x_j\rangle \langle x_j| dx_j = 1$ を分割された演算子の間に挿入し，伝播核を $N$ 個の短時間伝播核の積分として表せ。ただし，$x_0 = x_i$，$x_N = x_f$ とする。

(2) $\epsilon$ が十分に小さいとき，トロッター公式の簡易形である $\exp\left(-i(\hat{K}+\hat{V})\epsilon/\hbar\right) \approx \exp\left(-i\hat{K}\epsilon/\hbar\right) \exp\left(-i\hat{V}\epsilon/\hbar\right)$ が成り立つ。これを用いて，1つの短時間伝播核を次のように変形せよ。

$$
\langle x_{j} | \exp\left(-i \hat{H} \epsilon / \hbar\right) | x_{j-1} \rangle \approx \langle x_{j} | \exp\left(-i \hat{K} \epsilon / \hbar\right) | x_{j-1} \rangle \exp\left(-i V(x_{j-1}) \epsilon / \hbar\right)
$$

さらに，運動量固有状態の完全性関係 $\int |p_j\rangle \langle p_j| dp_j = 1$ および自由粒子の波動関数 $\langle x | p \rangle = \frac{1}{\sqrt{2\pi\hbar}} \exp\left(i p x / \hbar\right)$ を挿入して $p_j$ のガウス積分を実行し，短時間伝播核の形を導け。必要であれば，以下の積分公式を利用せよ。

$$
\int_{-\infty}^{\infty} \exp\left(-A p^2 + B p\right) dp = \sqrt{\frac{\pi}{A}} \exp\left(B^2 / 4A\right)
$$

(3) (2) の結果を (1) の全空間積分へ代入し，指数部分を $\epsilon \sum_{j=1}^{N}$ の形式にまとめよ。 $\epsilon \to 0$ の極限において，この和が位置 $x(t)$ と速度 $\dot{x}(t)$ を用いた時間積分，すなわち古典力学の作用 $S = \int L dt$ を用いた次の経路積分の形式にまとめられることを示せ。

$$
K(x_f, t_f; x_i, t_i) = \int \mathcal{D}[x(t)] \exp\left(i S[x(t)] / \hbar\right)
$$

---

## 解説

### (1)

時間発展演算子を $N$ 個の因数に並べた式に対し，それぞれの積の間に合計 $N-1$ 個の完全系を挿入します。

$$
K(x_f,t_f;x_i,t_i)
=\left\langle x_f\left|
\left(\exp\left(-i\hat H\epsilon/\hbar\right)\right)^N
\right|x_i\right\rangle
$$

$$
\begin{aligned}
K(x_f,t_f;x_i,t_i)
&=\int\cdots\int
\langle x_N|\exp\left(-i\hat H\epsilon/\hbar\right)|x_{N-1}\rangle\cdots\\
&\quad\times\langle x_1|\exp\left(-i\hat H\epsilon/\hbar\right)|x_0\rangle
\prod_{j=1}^{N-1}dx_j
\end{aligned}
$$

境界の条件を $x_0 = x_i$ ， $x_N = x_f$ と書き換えて，総乗の記号 $\prod$ でまとめます。

$$
\begin{aligned}
K(x_f,t_f;x_i,t_i)
&=\finalanswer{\int_{-\infty}^{\infty}\cdots\int_{-\infty}^{\infty}
\prod_{j=1}^{N}
\langle x_j|\exp\left(-i\hat H\epsilon/\hbar\right)|x_{j-1}\rangle}\\
&\quad\finalanswer{\times\prod_{j=1}^{N-1}dx_j}
\end{aligned}
$$

これにより，全体の伝播核が短時間伝播核の多重積分に分解されました。

### (2)

提示された近似式を用いて，ポテンシャルエネルギー演算子 $\hat{V}$ を右側の状態 $\langle x_{j-1} |$ に作用させて固有値 $V(x_{j-1})$ に置き換えます。

$$
\langle x_{j} | \exp\left(-i \hat{H} \epsilon / \hbar\right) | x_{j-1} \rangle \approx \langle x_{j} | \exp\left(-i \hat{K} \epsilon / \hbar\right) | x_{j-1} \rangle \exp\left(-i V(x_{j-1}) \epsilon / \hbar\right)
$$

残った運動エネルギーの部分の間に，運動量の完全系を挿入します。

$$
\langle x_{j} | \exp\left(-i \hat{K} \epsilon / \hbar\right) | x_{j-1} \rangle = \int_{-\infty}^{\infty} \langle x_j | p_j \rangle \langle p_j | \exp\left(-i \hat{p}^2 \epsilon / 2m\hbar\right) | x_{j-1} \rangle dp_j
$$

演算子 $\hat{p}$ を固有値 $p_j$ に変え，平面波の関数を代入します。

$$
\begin{aligned}
\langle x_j|\exp\left(-i\hat K\epsilon/\hbar\right)|x_{j-1}\rangle
&=\frac{1}{2\pi\hbar}\int_{-\infty}^{\infty}
\exp\Biggl[-\frac{i\epsilon}{2m\hbar}p_j^2
+\frac{i(x_j-x_{j-1})}{\hbar}p_j\Biggr]dp_j
\end{aligned}
$$

提示された公式を適用するため，係数を $A = \frac{i\epsilon}{2m\hbar}$ ， $B = \frac{i(x_j - x_{j-1})}{\hbar}$ と置きます。

$$
\begin{aligned}
\sqrt{\frac{\pi}{A}}
&=\sqrt{\frac{2\pi m\hbar}{i\epsilon}},\\
\frac{1}{2\pi\hbar}\sqrt{\frac{\pi}{A}}
&=\sqrt{\frac{m}{2\pi i\hbar\epsilon}}
\end{aligned}
$$

$$
\begin{aligned}
\frac{B^2}{4A} &= \frac{ -\frac{(x_j - x_{j-1})^2}{\hbar^2} }{ \frac{4i\epsilon}{2m\hbar} } \\
&= -\frac{m(x_j - x_{j-1})^2}{2i\hbar\epsilon} \\
&= \frac{i m (x_j - x_{j-1})^2}{2\hbar\epsilon}
\end{aligned}
$$

これらをまとめてポテンシャルの因子と掛け合わせると，短時間伝播核が得られます。

$$
\begin{aligned}
\langle x_j|\exp\left(-i\hat H\epsilon/\hbar\right)|x_{j-1}\rangle
&=\sqrt{\frac{m}{2\pi i\hbar\epsilon}}
\exp\left\{\frac{i\epsilon}{\hbar}\left[
\frac{m}{2}\left(\frac{x_j-x_{j-1}}{\epsilon}\right)^2
-V(x_{j-1})\right]\right\}
\end{aligned}
$$

### (3)

(2) の結果を (1) の多重積分に代入します。

$$
\begin{aligned}
K(x_f,t_f;x_i,t_i)
&=\int\cdots\int
\left(\frac{m}{2\pi i\hbar\epsilon}\right)^{N/2}\\
&\quad\times\exp\left\{\frac{i\epsilon}{\hbar}
\sum_{j=1}^{N}\left[
\frac{m}{2}\left(\frac{x_j-x_{j-1}}{\epsilon}\right)^2
-V(x_{j-1})\right]\right\}
\prod_{j=1}^{N-1}dx_j
\end{aligned}
$$

ここで $\epsilon \to 0$ の極限をとると，差分表現は微分へと移行します。

$$
\frac{x_j - x_{j-1}}{\epsilon} \to \dot{x}(t), \quad V(x_{j-1}) \to V(x(t))
$$

括弧の内部はラグランジアン $L(x, \dot{x}) = \frac{1}{2}m\dot{x}^2 - V(x)$ となり，時間に関する和の記号は積分へと変化します。

$$
\begin{aligned}
\epsilon\sum_{j=1}^{N}\left[
\frac{m}{2}\left(\frac{x_j-x_{j-1}}{\epsilon}\right)^2
-V(x_{j-1})\right]
&\longrightarrow\int_{t_i}^{t_f}L(x,\dot x)\,dt\\
&=S[x(t)]
\end{aligned}
$$

積分測度を $\mathcal{D}[x(t)] = \lim_{N \to \infty} \left( \frac{m}{2\pi i \hbar \epsilon} \right)^{N/2} dx_1 \cdots dx_{N-1}$ と定義することで，求める形式が得られます。

$$
\finalanswer{K(x_f, t_f; x_i, t_i) = \int \mathcal{D}[x(t)] \exp\left(i S[x(t)] / \hbar\right)}
$$
